services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=8000
      - LOG_LEVEL=INFO
      - RATE_LIMIT_PER_MINUTE=10
    restart: unless-stopped
